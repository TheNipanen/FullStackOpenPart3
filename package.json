{
  "name": "fullstackopenpart3",
  "version": "1.0.0",
  "description": "Phone book backend",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "dev": "nodemon index.js",
    "test": "echo \"Error: no test specified\" && exit 1",
    "build:ui": "rm -rf build && cd ../FullStackOpen/part2/puhelinluettelo/ && npm run build && cp -r build ../../../FullStackOpenPart3",
    "deploy": "git push heroku master",
    "deploy:full": "npm run build:ui && git add . && git commit -m uibuild && git push && npm run deploy",
    "lint": "eslint .",
    "logs:prod": "heroku logs --tail"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/TheNipanen/FullStackOpenPart3.git"
  },
  "author": "Niilo Lappi",
  "license": "UNLICENCED",
  "bugs": {
    "url": "https://github.com/TheNipanen/FullStackOpenPart3/issues"
  },
  "homepage": "https://github.com/TheNipanen/FullStackOpenPart3#readme",
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.0.1",
    "express": "^4.18.1",
    "mongoose": "^6.4.5",
    "morgan": "^1.10.0"
  },
  "devDependencies": {
    "eslint": "^8.20.0",
    "nodemon": "^2.0.19"
  }
}
